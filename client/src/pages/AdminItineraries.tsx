import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { Check, Compass, GripVertical, Loader2, MapPin, Plus, Save, X } from "lucide-react";
import { useState } from "react";

export default function AdminItineraries() { const utils = trpc.useUtils(); const { data: routes, isLoading } = trpc.admin.officialItineraries.useQuery({ slug: "adamantina" }); const { data: catalog } = trpc.catalog.list.useQuery({ slug: "adamantina" }); const create = trpc.admin.createOfficialItinerary.useMutation({ onSuccess: () => { utils.admin.officialItineraries.invalidate(); setForm({ title: "", description: "" }); setSelected([]); setOpen(false); } }); const [open, setOpen] = useState(false); const [form, setForm] = useState({ title: "", description: "" }); const [selected, setSelected] = useState<number[]>([]); const toggle = (id: number) => setSelected((items) => items.includes(id) ? items.filter((item) => item !== id) : [...items, id]); const submit = async () => { if (!form.title || !selected.length) return; await create.mutateAsync({ slug: "adamantina", title: form.title, description: form.description || undefined, items: selected.map((placeId, position) => ({ placeId, position })) }); };
  return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><Badge className="border-0 bg-[#e2f0e3] text-[10px] font-bold uppercase tracking-[0.16em] text-[#0d5c4d]">Curadoria municipal</Badge><h2 className="mt-3 font-display text-3xl font-bold tracking-[-0.05em] text-[#123f36]">Roteiros oficiais</h2><p className="mt-2 text-sm text-[#71827c]">Organize experiências e publique caminhos recomendados pela Secretaria.</p></div><Button onClick={() => setOpen(true)} className="rounded-xl bg-[#0d5c4d] font-bold text-white"><Plus className="mr-2 h-4 w-4" /> Novo roteiro</Button></div>{open && <Card className="rounded-[22px] border-[#cfe0d2] bg-white"><CardHeader className="flex flex-row items-center justify-between"><CardTitle className="font-display text-xl text-[#123f36]">Publicar roteiro oficial</CardTitle><button onClick={() => setOpen(false)}><X className="h-4 w-4 text-[#71827c]" /></button></CardHeader><CardContent><div className="grid gap-4 lg:grid-cols-2"><label className="space-y-1.5"><span className="text-xs font-bold uppercase tracking-wide text-[#71827c]">Nome do roteiro</span><Input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} placeholder="Adamantina em um dia" /></label><label className="space-y-1.5"><span className="text-xs font-bold uppercase tracking-wide text-[#71827c]">Descrição</span><Input value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} placeholder="Uma experiência para conhecer…" /></label></div><div className="mt-5"><p className="text-xs font-bold uppercase tracking-wide text-[#71827c]">Selecione e ordene os pontos</p><div className="mt-3 grid gap-2 md:grid-cols-2">{catalog?.places?.length ? catalog.places.map((place) => <button key={place.id} onClick={() => toggle(place.id)} className={`flex items-center gap-3 rounded-xl border p-3 text-left ${selected.includes(place.id) ? "border-[#7fb4a0] bg-[#eef6ef]" : "border-[#edf1ed]"}`}><GripVertical className="h-4 w-4 text-[#a6b5ac]" /><MapPin className="h-4 w-4 text-[#0d5c4d]" /><span className="flex-1 text-sm font-semibold">{place.name}</span>{selected.includes(place.id) && <Check className="h-4 w-4 text-[#0d5c4d]" />}</button>) : <p className="rounded-xl bg-[#f5f8f5] p-5 text-sm text-[#71827c]">Cadastre locais aprovados antes de criar um roteiro.</p>}</div></div><div className="mt-5 flex justify-end border-t border-[#edf1ed] pt-4"><Button onClick={submit} disabled={create.isPending || !form.title || !selected.length} className="rounded-xl bg-[#0d5c4d] font-bold text-white">{create.isPending ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <Save className="mr-2 h-4 w-4" />} Publicar roteiro</Button></div></CardContent></Card>}<Card className="rounded-[22px] border-[#e0e8e0] bg-white"><CardHeader><CardTitle className="font-display text-xl text-[#123f36]">Roteiros publicados <span className="ml-2 text-sm font-normal text-[#92a099]">{routes?.length ?? 0}</span></CardTitle></CardHeader><CardContent>{isLoading ? <Loader2 className="h-5 w-5 animate-spin text-[#0d5c4d]" /> : routes?.length ? <div className="divide-y divide-[#edf1ed]">{routes.map((route) => <div key={route.id} className="flex items-center gap-3 py-4"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#e8f1ea] text-[#0d5c4d]"><Compass className="h-5 w-5" /></div><div className="flex-1"><p className="font-bold text-[#20332f]">{route.title}</p><p className="mt-1 text-xs text-[#8a9a92]">{route.description ?? "Sem descrição"}</p></div><Badge className="border-0 bg-[#e2f0e3] text-[10px] font-bold uppercase text-[#0d5c4d]">Publicado</Badge></div>)}</div> : <div className="py-8 text-center text-sm text-[#71827c]">Nenhum roteiro oficial publicado.</div>}</CardContent></Card></div>; }
